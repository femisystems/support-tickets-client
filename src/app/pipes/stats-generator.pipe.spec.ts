import { StatsGeneratorPipe } from './stats-generator.pipe';

describe('StatsGeneratorPipe', () => {
  it('create an instance', () => {
    const pipe = new StatsGeneratorPipe();
    expect(pipe).toBeTruthy();
  });
});
